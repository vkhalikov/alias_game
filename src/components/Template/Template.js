import React from 'react';
import PropTypes from 'prop-types';
import classNames from 'classnames';
import './Template.css';

const Template = () => {
  const finalClassName = classNames('LocalModePage',);
  return null;
};

Template.propTypes = {

};

export default Template;
